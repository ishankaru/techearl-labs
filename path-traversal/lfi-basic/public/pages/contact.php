<h2>Contact</h2>
<p>The second of the two innocuous partials shipped with the lab. Hit <code>?page=contact</code> to confirm the include() endpoint reaches files inside <code>pages/</code> as designed.</p>
