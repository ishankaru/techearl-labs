<h2>About</h2>
<p>A small landing page that view.php and view-raw.php render as their default partial. Nothing sensitive here; it exists only so the intended behaviour of the include() endpoint is visible.</p>
<p>The interesting traffic in this lab goes to the other query strings the README enumerates.</p>
